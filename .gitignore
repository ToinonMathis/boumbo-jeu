# Node.js — on ne versionne JAMAIS ça
node_modules/
npm-debug.log*

# Cache local des quiz (filet de secours hors-ligne, propre à chaque machine)
.cache-quiz/

# Fichiers système
.DS_Store
Thumbs.db

# Arduino — fichiers de compilation temporaires
*.hex
build/

# Config locale de l'outil Claude Code — spécifique à la machine
.claude/settings.local.json

# Secrets / config locale — jamais versionnés
.env
.env.local